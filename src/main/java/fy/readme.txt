- modules
    - code change data  (CCD)
    - code change slice (CCS)
    - graph definition  (GD)
    - graph build       (GB)

- todo
   - data flow tracking of a single variable.
   - in CFG analysis, the parent(containing) node of
   else branch seems should be if branch node.
